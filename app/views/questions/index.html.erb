<% provide :page_title, 'Questions' %>
<% currently_at "Questions" %>

<h1 class="mb-4">Questions</h1>

<section class="mb-5">
  <h2>Search by tags</h2>
  <%= form_with url: questions_path, method: :get do |f| %>
    <%= f.collection_select :tag_ids, @tags, :id, :title, {selected: params[:tag_ids]}, multiple: true, required: true, class: 'js-multiple-select w-100' %>
    <%= f.submit 'Submit', class: 'btn btn-primary' %>
  <% end %>
</section>

<%= link_to "New question", new_question_path, class: 'btn btn-primary btn-lg' %>
<%= paginate @questions %>

<%= render @questions %>

<%= paginate @questions %>
