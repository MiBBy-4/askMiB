<h1>Questions</h1>
<% @questions.each do |question| %>
  <h3><%= question.title %></h3>
  <time datetime="<%= question.formatted_create_at %>"><%= question.formatted_create_at %></time>
  <p><%= question.body %><p>
<% end %>